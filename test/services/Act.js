'use strict';

// Jasmine, Chai and Angular Mocks will be global

var expect = chai.expect;

beforeEach(module('ngFH'));

describe('Act', function () {

  describe('#callFn', function () {

    it('Should call a cloud function', inject(function (Act) {

    }));

  });

  describe('#getDefaultTimeout', function () {

    it('Should return the default timeout value', inject(function (Act) {

    }));

  });

  describe('#setDefaultTimeout', function () {

    it('Set the default timeout to a new value', inject(function (Act) {

    }));

  });

});
